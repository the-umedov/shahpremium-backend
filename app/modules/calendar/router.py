"""calendar.module.js ekvivalenti.

MUHIM: asl loyihada `calendar.module.js` bo'sh stub — hech qanday controller,
service yoki provider yo'q (grep bilan tasdiqlangan). Shu sababli bu yerda
ham faqat bo'sh router e'lon qilinadi, hech qanday endpoint qo'shilmaydi."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["calendar"])
